@model StoreDto
@{
    ViewData["Title"] = "Delete";
}

<h2>Delete Store</h2>

<h3>Are you sure you want to delete this?</h3>

<form asp-action="Delete">      
    <div>
        <input hidden  asp-for="Id" value="@Model.Id" />
              <label class="fw-bold" asp-for="Name">Store Name:</label>
        <input readonly   asp-for="Name" value="@Model.Name" />
              <label asp-for="Address">Address:</label>
        <input readonly  asp-for="Address" value="@Model.Address" />
        </div>
        <div class="m-3">
            <input type="submit" value="Delete" class="btn btn-danger" /> |
    <a asp-controller="Home" asp-action="Index" class="btn btn-danger">Back</a>
        </div>
    </form>
</div>
